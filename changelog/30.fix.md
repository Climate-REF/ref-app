Allowed the API to run against a read-only `/ref` volume via a new `REF_READ_ONLY_DATABASE` setting,
using `climate-ref` 0.13.1's `Database.from_config(read_only=True)` and `Database.migration_status` helpers.
Bumped `vite` to `>=7.3.2` for a security fix and refreshed the Python lockfile.
