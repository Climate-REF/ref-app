Fixed page view analytics being discarded on the container deployments.
The analytics proxy now forwards the visitor's real IP rather than the Cloudflare edge,
which Plausible was filtering out as data centre traffic.
