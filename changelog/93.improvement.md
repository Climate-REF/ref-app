Execution and execution group counts on a diagnostic page now cover the same runs as the values below them.
Both are scoped to the diagnostic version currently in use, so a page can no longer report thousands of successful runs beside an empty table.
The version itself is shown alongside the counts.
