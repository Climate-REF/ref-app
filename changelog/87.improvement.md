Drops the model-family grouping behind the sparse-sample warning.
The warning now counts distinct `source_id` values, so a chart drawn from fewer than ten models is
flagged whatever centres they came from.
