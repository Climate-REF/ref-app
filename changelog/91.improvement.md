Reported resource usage in core hours instead of CPU time formatted as days.
Reported the peak memory of a diagnostic as a min and max range, which shows how widely executions of the same diagnostic vary.
