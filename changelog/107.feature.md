Added a model view at `/models`.
Each model gets a page summarising the runs it took part in, which diagnostics it failed, and where its scalar metrics sit against the ensemble that reported them.
The pages are not linked from the navigation yet, because the layout still needs a pass against real data.
